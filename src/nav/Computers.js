import React from 'react'
import { Layout } from '../imports'

const Computers = () => {
    return (
        <Layout>
          <h1>Computers</h1>  
        </Layout>
    )
}

export default Computers
